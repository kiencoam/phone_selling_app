// This component is no longer used as its functionality is incorporated into Header.jsx
// Keeping this file as a placeholder in case we need to restore it in the future

import React from 'react';

const SearchBar = () => {
  return null;
};

export default SearchBar;